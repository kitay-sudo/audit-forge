# Security Audit Report — `<PROJECT NAME>`

> Generated by an AI agent following [AuditForge](./AUDITFORGE.md) v1.1.
> **Date:** `<YYYY-MM-DD>` · **Auditor (agent/model):** `<name>` · **Commit/ref:** `<git sha>`
> **Scope authorization:** `<who authorized this audit / engagement reference>`

---

## 1. Executive summary

`<5 sentences max: overall risk posture, what the system is, the most dangerous issue, and the headline
recommendation.>`

**Findings by severity:**

| Critical | High | Medium | Low | Info |
|:--------:|:----:|:------:|:---:|:----:|
|    0     |  0   |   0    |  0  |  0   |

**Top 3 things to fix first:**
1. `<finding + why>`
2. `<finding + why>`
3. `<finding + why>`

**Overall production-readiness verdict:** `✅ Go` / `⚠️ Go with conditions` / `❌ No-go` — `<one line>`

---

## 2. System overview & coverage

- **Purpose:** `<what the app does, who uses it>`
- **Stack:** `<language(s), framework(s), DB, cache, queue, hosting>`
- **Entry points:** `<HTTP/GraphQL/WS/jobs/CLI/webhooks count + notable ones>`
- **Crown-jewel assets:** `<PII / payment / secrets / regulated data>`
- **AuthN/AuthZ model:** `<sessions/JWT/OAuth; roles; multi-tenancy>`
- **External egress:** `<email, payment, analytics, AI/LLM, other 3rd-party>`

### Coverage statement (what was and wasn't audited)

| Phase | Audited? | Notes / why skipped |
|-------|:--------:|---------------------|
| 0 Recon & threat model | ✅ |  |
| 1 Dependencies / supply chain | ✅ / ⚠️ / ❌ |  |
| 2 Secrets & configuration | | |
| 3 Routes & attack surface | | |
| 4 AuthN & AuthZ | | |
| 5 Input validation & injection | | |
| 6 Business / logic flaws | | |
| 7 Database layer | | |
| 8 Data egress / outputs | | |
| 9 Rate limiting & DoS | | |
| 10 Crypto & secrets mgmt | | |
| 11 Files & storage | | |
| 12 Client / frontend | | |
| 13 Infra / CI-CD / IaC | | |
| 14 Observability & IR | | |
| 15 AI / LLM / agentic *(if applicable)* | ✅ / ⚠️ / ❌ / N/A | |
| 16 Mobile / MASVS *(if applicable)* | ✅ / ⚠️ / ❌ / N/A | |
| 17 Privacy & compliance | | |

---

## 3. Findings

> Ordered by severity (Critical → Info). Duplicate the block per finding. Delete this note.

### [SEVERITY] `<short title>`  (Phase `<N>` · OWASP `<A0X / APIx>` · CWE-`<XXX>`)

**Location:** `path/to/file.ext:LINE-LINE`
**Category:** `<e.g., Broken Object-Level Authorization>`
**Status:** `Confirmed` | `Needs-verification`
**CVSS (High/Critical only):** `<vector + score, optional>`

**Description.** `<What the flaw is and why it is exploitable in THIS codebase.>`

**Exploit path.** `<Source → sink. Which trust boundary is crossed. A conceptual proof-of-concept input
(do not attack systems you don't own).>`

**Impact.** `<What an attacker gains: data, access, money, downtime.>`

**Recommendation.** `<The fix in prose; prefer framework-native solutions.>`

**Fix (diff).**
```diff
--- a/path/to/file.ext
+++ b/path/to/file.ext
@@
- vulnerable line
+ fixed line
```

**Verification.** `<Test to add or command to run to confirm the fix and guard against regression.>`

---

## 4. Prioritized remediation plan (effort × impact)

| # | Finding | Severity | Effort | Impact | Owner | Status |
|---|---------|:--------:|:------:|:------:|-------|--------|
| 1 |         | Critical |  S/M/L | High   |       | open   |

**Quick wins (low effort, high impact):** `<list>`
**Larger remediations:** `<list>`

---

## 5. Dependency / supply-chain results

| Package@version | Advisory | Severity | Fixed in | Upgrade risk | Action |
|-----------------|----------|:--------:|----------|--------------|--------|
|                 |          |          |          |              |        |

`<paste raw SCA tool output in the appendix>`

---

## 6. Route / attack-surface inventory

| Method+Path / Handler | Auth? | Roles | Input sources | Touches (DB/FS/net/$) | Findings |
|-----------------------|:-----:|-------|---------------|-----------------------|----------|
|                       |       |       |               |                       |          |

---

## 7. Production-readiness gate

> Mark each ✅ pass / ⚠️ partial / ❌ fail. Mirrors AuditForge §23.

- [ ] No open Critical/High (or documented, accepted, owned risk)
- [ ] No secrets in code or git history; secret manager in use; rotation possible
- [ ] No known-exploitable dependency CVEs
- [ ] Supply-chain integrity: lockfile committed, deps/actions pinned, SBOM generated, artifacts signed
- [ ] Strong AuthN; AuthZ enforced server-side on every route
- [ ] No IDOR/BOLA — every object access ownership-checked
- [ ] All untrusted input validated; no reachable injection sinks
- [ ] Rate limiting on auth / email / expensive / public endpoints (shared store)
- [ ] Payload-size limits, pagination caps, timeouts, pool limits
- [ ] Security headers + HTTPS/HSTS + secure cookies
- [ ] No info leakage in errors; debug off in prod
- [ ] Sensitive data encrypted at rest; PII minimized; logs scrubbed
- [ ] DB least-privilege; backups exist & protected
- [ ] Security logging + monitoring + alerting
- [ ] Infra hardened (no public DB/admin, least-priv IAM, non-root containers, safe CI secrets)
- [ ] **(AI apps)** Prompt-injection guardrails; LLM output untrusted; tools least-privilege; token/cost caps
- [ ] **(If PII/regulated)** Data-flow mapped; retention + deletion; consent & GDPR/PCI/HIPAA obligations
- [ ] Tests cover security fixes; CI runs SCA

**Blocking items (if any):**
1. `<...>`

---

## 8. Appendix

- Raw tool outputs (SCA, SAST, IaC scan).
- Notes, assumptions, and items needing human verification.
- Residual-risk register (accepted risks + owner + review date).
